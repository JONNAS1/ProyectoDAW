// src/environments/environment.ts
export const environment = {
  production: false,
  // URL base de la API PHP (XAMPP, WAMP, etc.)
  // Ajustar segun tu servidor local
  apiUrl: 'http://localhost/chuletarium/backend/api',
};
